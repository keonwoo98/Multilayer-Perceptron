# Python
__pycache__/
*.py[cod]
*.egg-info/

# 가상환경
venv/
.venv/
env/

# 학습 산출물
models/
*.npz
*.npy

# split 결과 (재생성 가능)
data_train.csv
data_valid.csv
data_training.csv
data_test.csv

# OS
.DS_Store

# 학습 산출물 (재생성 가능)
learning_curves.png

# 평가 자료 (git에 올리지 않음 — 42 제공, 우리 제출물 아님)
Intra Projects multilayer-perceptron Edit.pdf
Multilayer Perceptron.pdf
evaluation.py

# TTS 학습 도구 (개인용 — 키와 생성 음성은 올리지 않음)
.env
*.mp3

# 개인 학습 자료 (디펜스 대본·TTS 등 — git에 올리지 않음, 로컬 보관)
docs/
