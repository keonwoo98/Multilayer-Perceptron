# 🎤 디펜스 구어체 대본 (그대로 읽기)

> **사용법**
> - 💬 표시 줄 = **실제로 말할 대사**. 그대로 소리 내어 읽으면 된다.
> - `코드블록` = 터미널에 칠 명령어.
> - 〔 〕 = 행동 지시 (말 안 함).
> - 평가자가 중간에 물으면 PART 4에서 해당 답을 찾아 읽는다.

---

# PART 0. 시작 전 마음가짐 (혼잣말)

- 성능 점수(test BCE)는 5점 만점 중 일부이고 split 운이 절반이다. 4점 나와도 담담하게.
- 진짜 점수는 **알고리즘 설명 4개(20점)** 다. PART 3을 입에 붙이는 게 핵심.
- 막히면 "모르겠습니다"보다 **아는 데까지 + 비유로** 말하면 부분 점수가 나온다.

---

# PART 1. 오프닝 (시작하면서 이렇게)

💬 "안녕하세요. 제 프로젝트는 multilayer perceptron, 다층 퍼셉트론이에요. 텐서플로우나 사이킷런 같은 머신러닝 라이브러리 없이 numpy로 처음부터 직접 구현했습니다."

💬 "하는 일은, 위스콘신 유방암 데이터에서 세포핵 특징 30개를 받아서 이 종양이 악성인지 양성인지 분류하는 거예요. 악성이면 M, 양성이면 B."

💬 "구조는 기본이 입력 30개, 은닉층 24개짜리 두 개, 출력 2개예요. 은닉층은 sigmoid를 쓰고, 출력층은 softmax를 써서 '양성일 확률, 악성일 확률' 이렇게 합이 1인 확률로 내보냅니다."

💬 "크게 세 프로그램이 있어요. 데이터를 나누는 split, 학습하는 train, 예측하고 평가하는 predict. train이 backpropagation이랑 gradient descent로 학습해서 모델을 저장하고, predict가 그 모델을 불러와서 binary cross-entropy로 평가합니다. 역전파가 맞는지는 gradient checking으로 검증도 했어요."

💬 "그럼 먼저 실행해서 보여드릴게요."

---

# PART 2. 시연하며 말하기 (명령어 + 대사)

〔가상환경은 활성화 안 하고 `./venv/bin/python`으로 가는 게 제일 안전. 활성화하려면 점 없이 `source venv/bin/activate`〕

## 2-1. 데이터 분리 프로그램

💬 "먼저 데이터를 학습용이랑 검증용으로 나누는 프로그램이에요."
```bash
python3 split.py data.csv --seed 42
```
💬 "569명을 섞어서 456명은 학습용 data_train, 113명은 검증용 data_valid로 나눴어요. 섞는 이유는, 원본 데이터가 앞쪽에 악성이 몰려 있어서 안 섞고 자르면 한쪽에 쏠리거든요. seed를 고정한 건 매번 같은 결과가 나오게 하려고요."

## 2-2. 평가용 데이터 생성 (evaluation.py)

💬 "이건 42에서 제공하는 평가용 스크립트예요. 제가 만든 게 아니라, 공정한 채점을 위해 모두 똑같은 방식으로 나누는 거예요."
```bash
python3 evaluation.py
```
💬 "data.csv를 섞어서 75%는 data_training, 25%는 data_test로 나눠요. 여기서 중요한 건, 이 25% test는 모델이 학습 때 한 번도 안 보는 데이터라는 거예요. 진짜 실력은 이 안 본 데이터로 재야 하거든요."

## 2-3. 학습

💬 "이제 학습입니다."
```bash
python3 train.py data_training.csv
```
💬 "매 epoch마다 loss랑 val_loss가 같이 줄어드는 게 보이시죠. loss는 학습 데이터 오차고 val_loss는 검증 데이터 오차예요. 둘 다 줄어들면 학습이 잘 되고 있는 거고요."

💬 "지금 early stopping이 걸렸네요. 검증 오차가 더 안 좋아지면 멈추고, 가장 좋았던 가중치로 되돌리는 기능이에요. 과적합을 막아줍니다."

〔학습곡선 창이 뜬다〕
💬 "여기 학습곡선이에요. 왼쪽이 loss, 오른쪽이 accuracy. 각각 파란색이 학습, 주황색이 검증인데, 두 선이 같이 붙어서 내려가죠. 이게 과적합이 안 났다는 뜻이에요. 만약 학습 선만 내려가고 검증 선이 벌어지면 외운 거라 과적합이거든요."
〔창을 닫는다 — 닫아야 다음 명령으로 넘어감〕

## 2-4. 예측·평가 (★ 진짜 점수)

💬 "이제 학습한 모델로, 아까 안 본 data_test로 예측하고 평가합니다."
```bash
python3 predict.py --data data_test.csv
```
💬 "binary cross-entropy가 나오죠. 이게 성능 점수예요. 낮을수록 좋고요. accuracy랑 precision, recall, f1도 같이 보여줍니다."

〔5점(0.08 미만)을 노리는데 점수가 아쉬우면〕
💬 "학습이 무작위 요소가 있어서 매번 조금씩 달라지는데, 규칙상 최대 세 번까지 학습해서 제일 좋은 걸 쓸 수 있어요."
```bash
python3 train.py data_training.csv --seed 1
python3 predict.py --data data_test.csv
```

## 2-5. 모듈러성 (인자로 구조 바꾸기)

💬 "구조는 인자로 자유롭게 바꿀 수 있어요. 은닉층을 세 개로 해볼게요."
```bash
python3 train.py data_training.csv --layers 24 24 24 --epochs 40
python3 -c "import numpy as np; print(np.load('models/model.npz')['layer_sizes'])"
```
💬 "저장된 구조를 보면 30, 24, 24, 24, 2로 은닉층이 세 개가 됐죠. 코드를 안 고치고 인자만으로 바꿀 수 있어요."

## 2-6. 보너스

💬 "보너스로 Adam optimizer도 구현했어요."
```bash
python3 train.py data_training.csv --optimizer adam --lr 0.001 --epochs 40
```
💬 "early stopping도 기본으로 켜져 있는데, 끄려면 이렇게요."
```bash
python3 train.py data_training.csv --no_early_stopping --epochs 30
```
💬 "그리고 precision, recall, f1 같은 추가 지표는 아까 predict 출력에 이미 나왔고요. 미니배치랑 L2 정규화도 보너스로 들어가 있어요."

## 2-7. 예외처리

💬 "없는 파일을 넣어도 크래시 안 나고 깔끔하게 처리해요."
```bash
python3 predict.py --data 없는파일.csv
```
💬 "이렇게 에러 메시지만 나오고 종료됩니다."

〔채점 직전엔 기본 설정으로 다시 학습하고 predict 하기〕
```bash
python3 train.py data_training.csv
python3 predict.py --data data_test.csv
```

---

# PART 3. ⭐ 알고리즘 설명 (핵심 20점 — 그대로 읽기)

> 여기가 점수의 핵심이다. 천천히, 비유부터 시작해서 설명한다.

## 3-1. Feedforward

💬 "feedforward는 한마디로, 세포 특징 숫자 30개를 넣으면 악성일 확률이 나오는 과정이에요. 데이터가 입력에서 출력으로 앞으로만 흘러서 feedforward라고 불러요."

💬 "가장 작은 단위인 뉴런 하나부터 말씀드릴게요. 뉴런 하나는 판사 같은 거예요. 여러 증거, 그러니까 입력들을 받아서 각각 얼마나 중요한지 가중치를 곱해서 다 더해요. 이걸 가중합이라고 하는데, 예를 들어 입력이 두 개면 z는 x1 곱하기 w1, 더하기 x2 곱하기 w2, 더하기 bias, 이렇게 됩니다."

💬 "그 다음에 이 z에 활성화 함수를 씌워요. 저는 은닉층에 sigmoid를 썼는데, sigmoid는 어떤 값이든 0과 1 사이로 눌러주는 S자 함수예요. 예를 들어 z가 3.8이면 sigmoid를 씌우면 0.978 정도가 나와요. 1에 가까우니까 강하게 반응한다는 뜻이고요."

💬 "뉴런 하나는 출력이 하나잖아요. 이걸 여러 개 나란히 세우면 층이 돼요. 여기서 제일 중요한 게, 각 뉴런은 입력을 전부 다 받아요. 1대1로 짝지어지는 게 아니라요. 입력이 30개면 뉴런 하나가 가중치를 30개 가져요. 그리고 뉴런이 24개면 그 층은 출력이 24개 나오고요."

💬 "그래서 한 층의 계산은 결국 행렬 곱 한 번이에요. 가중치 행렬 W 곱하기 입력 x, 더하기 bias, 거기에 활성화. W의 크기는 뉴런 수 곱하기 입력 수가 되고요. 이래서 numpy 선형대수를 쓰는 거예요."

💬 "이 층을 여러 개 쌓는데, 한 층의 출력이 다음 층의 입력이 돼요. 그래서 숫자가 30개에서 24개로, 또 24개로, 마지막에 2개로 변해가요. 마지막 출력층만 softmax를 써서 두 개를 합이 1인 확률로 만들어요. 양성일 확률, 악성일 확률 이렇게요. 이게 feedforward입니다."

〔추가 질문 — "활성화 함수가 왜 필요해요?"〕
💬 "활성화 함수가 없으면 층을 아무리 쌓아도 결국 하나의 직선, 선형 변환으로 합쳐져 버려요. sigmoid 같은 비선형 함수를 끼워야 복잡한 패턴을 배울 수 있어요."

## 3-2. Gradient descent

💬 "gradient descent, 경사 하강법은 가중치를 어떻게 고쳐서 학습하느냐예요. 목표는 loss, 그러니까 틀린 정도를 제일 작게 만드는 거고요."

💬 "비유를 들면, 안개가 짙게 낀 산에서 가장 낮은 골짜기로 내려가는 거예요. 앞이 안 보이니까 발밑 기울기만 느낄 수 있죠. 그래서 지금 서 있는 곳에서 가장 가파르게 내려가는 방향으로 한 걸음 딛고, 또 느끼고 한 걸음 딛고, 이걸 반복하면 바닥에 도착해요."

💬 "여기서 기울기가 핵심인데, 기울기는 '이 가중치를 조금 늘리면 loss가 어느 쪽으로 변하나'예요. 기울기가 양수면 가중치를 늘릴 때 loss가 커진다는 거니까 가중치를 줄여야 하고, 음수면 반대로 늘려야 해요. 그래서 공식이, 새 가중치는 옛날 가중치 빼기 학습률 곱하기 기울기예요. 이 빼기 부호가 항상 기울기 반대 방향, 그러니까 내리막으로 가게 해줘요."

💬 "학습률은 한 걸음의 크기예요. 너무 크면 골짜기를 건너뛰어서 발산하고, 너무 작으면 너무 느려요. 그래서 적당히 잡아야 해요."

💬 "저는 미니배치 방식을 썼어요. 샘플 하나씩 업데이트하는 게 아니라 16개씩 묶어서 기울기를 평균낸 다음에 한 번 업데이트해요. 이게 더 안정적이고 과신을 줄여줘요."

〔추가 질문 — "L2 정규화가 뭐예요?"〕
💬 "가중치가 너무 커지면 페널티를 주는 거예요. 업데이트할 때 기울기에 가중치를 조금 더해서 가중치를 작게 유지해요. 이게 과신이랑 과적합을 막아줘요."

## 3-3. Backpropagation

💬 "backpropagation, 역전파는 아까 gradient descent에서 필요한 '각 가중치의 기울기'를 구하는 방법이에요. loss는 숫자 하나로 알지만, 그 loss에 각 가중치가 얼마나 책임이 있는지는 따로 계산해야 하거든요."

💬 "비유를 들면, 회사에서 프로젝트가 실패했어요. 그게 loss예요. 사장이 책임을 묻는데, 부장한테 '네 책임이 이만큼이다' 하고, 부장은 그걸 받아서 과장들한테 나눠주고, 과장은 또 사원한테 나눠줘요. 이렇게 책임이 뒤에서 앞으로, 출력에서 입력 쪽으로 거꾸로 흘러가요. 그래서 back, 뒤로 propagation, 전파, 역전파예요."

💬 "왜 거꾸로 가냐면, 첫째로 loss가 신경망 맨 끝 출력에서 계산되니까 거기서 시작해야 하고, 둘째로 앞쪽 층의 책임을 알려면 뒤쪽 층의 책임을 먼저 알아야 하기 때문이에요."

💬 "출발점이 재밌는데, 출력층의 오차가 그냥 예측 빼기 정답이에요. 예를 들어 예측이 양성 0.56, 악성 0.44인데 정답이 악성이면, 정답은 0과 1이니까 오차는 0.56, 그리고 마이너스 0.56이 돼요. 원래는 softmax랑 cross-entropy 미분을 복잡하게 해야 하는데, 이 둘이 수학적으로 짝이라서 계산하면 중간 과정이 다 약분되고 그냥 뺄셈 하나만 남아요."

💬 "그 다음에 이 오차를 거꾸로 전파하는데, 각 은닉 뉴런의 오차는 그 뉴런이 출력에 기여한 가중치 곱하기 출력 오차를 다 더한 거예요. 수식으로는 W 전치행렬 곱하기 오차, 거기에 sigmoid 미분을 곱하고요. 중요한 건 전달되는 게 loss 숫자가 아니라 오차 벡터라는 거예요. 그리고 층마다 값도 크기도 바뀌어요. 2개에서 24개로, 또 24개로요."

💬 "이렇게 거꾸로 한 바퀴 돌면 각 층 가중치의 기울기가 다 구해져요. 가중치 기울기는 그 층의 오차 곱하기 입력이고요. 그래서 forward 할 때 입력을 저장해 둬요, 나중에 backward에서 쓰려고요."

💬 "이게 맞는지 어떻게 검증했냐면, gradient checking이라는 걸 했어요. 가중치를 아주 조금 흔들어서 loss가 실제로 얼마나 변하는지를 직접 재고, 제 역전파가 계산한 기울기랑 비교했어요. 둘이 소수점 열 자리까지 일치했어요. 그래서 역전파가 정확하다고 확신했고요."

〔추가 질문 — "출력 오차가 왜 예측 빼기 정답이에요?"〕
💬 "softmax랑 cross-entropy가 짝꿍이라서 같이 미분하면 중간 항들이 상쇄돼요. 그래서 결과가 깔끔하게 예측 빼기 정답이 됩니다."

〔추가 질문 — "전달되는 게 같은 거예요?"〕
💬 "아니요. 오차 신호가 층마다 다시 계산돼서 값도 크기도 바뀌어요. 같은 게 흐르는 게 아니에요."

## 3-4. Overfitting

💬 "과적합은 모델이 학습 데이터를 외워버려서, 학습 데이터는 잘 맞히는데 안 본 데이터는 못 맞히는 상황이에요."

💬 "저는 이걸 프로젝트로 바로 보여드릴 수 있어요. 같은 모델로 학습에 쓴 data_training으로 예측하면 binary cross-entropy가 0.03 정도로 아주 낮아요. 근데 모델이 한 번도 안 본 data_test로 예측하면 0.15 정도로 훨씬 높아요. 같은 모델인데 다섯 배 차이가 나죠. 이 차이가 바로 과적합이에요. 학습 데이터는 외웠으니까 잘 맞히고, 안 본 데이터는 진짜 실력이라 더 어려운 거예요."

💬 "학습곡선으로도 볼 수 있는데, 학습 loss는 계속 내려가는데 검증 loss가 안 내려가고 벌어지기 시작하면 과적합 신호예요."

💬 "저는 세 가지로 막았어요. 첫째 L2 정규화로 가중치가 너무 커지는 걸 막아서 과신을 줄였고, 둘째 미니배치로 업데이트를 부드럽게 했고, 셋째 early stopping으로 검증 loss가 안 좋아지면 멈추고 제일 좋았던 가중치로 되돌렸어요."

💬 "사실 처음엔 과적합이 심했어요. 샘플 하나씩 오래 학습하니까 가중치가 커져서 모델이 과신을 했어요. 정확도는 96%인데 몇 개를 0.000으로 확신하면서 틀려서 test 점수가 0.33까지 폭발했어요. 그걸 위 세 가지로 0.08 근처까지 낮췄고요."

〔추가 질문 — "과소적합이랑 뭐가 달라요?"〕
💬 "과소적합은 모델이 너무 약해서 학습 데이터도 검증 데이터도 둘 다 못 맞히는 거고요, 과적합은 학습은 잘하는데 검증을 못하는, 너무 특화된 경우예요."

〔추가 질문 — "정확도는 높은데 왜 loss가 커요?"〕
💬 "정확도는 맞았냐 틀렸냐만 세요. 근데 cross-entropy는 확신하면서 틀린 걸 아주 세게 벌해요. 그래서 몇 개를 강하게 틀리면 정확도는 높아도 loss는 커져요."

---

# PART 4. 예상 질문 — 이렇게 답하기 (구어체)

> 평가자가 물으면 해당 답을 찾아 읽는다.

## 데이터에 대해

**"악성이랑 양성이 뭐예요?"**
💬 "악성은 암이에요. 빠르게 자라고 전이돼서 위험하고요. 양성은 암이 아닌 순한 종양이에요, 위험하지 않고요. 한자로 양성은 좋을 량 자를 써요. 참고로 검사에서 말하는 양성 음성, 그건 다른 한자고 저희 데이터엔 없어요."

**"데이터가 어떻게 생겼어요?"**
💬 "569명, 32개 컬럼이에요. 첫 번째는 id인데 환자 번호라 버리고요, 두 번째가 diagnosis, 정답이에요 M이나 B. 나머지 30개가 세포 특징, 입력이에요."

**"그 30개 feature가 뭔지 아세요?"**
💬 "세포핵의 측정값이에요. 반지름, 질감, 둘레, 면적 이런 10가지 측정값을 각각 평균, 표준오차, 최악값 세 가지로 잰 거라 30개예요. 근데 제 코드는 컬럼 이름이 아니라 위치로 처리해서 이름 자체에 의존하진 않아요."

**"어떤 feature가 중요한지 어떻게 골랐어요?"**
💬 "직접 안 골랐어요. 30개를 다 넣고 신경망이 학습하면서 가중치로 스스로 중요도를 정하게 했어요. 중요한 feature는 가중치가 커지고, 쓸모없는 건 0에 가까워져요. 다만 데이터 이해 차원에서 EDA로 어떤 게 잘 갈리는지 그래프로 보긴 했어요."

## 전처리에 대해

**"정규화를 왜 했어요?"**
💬 "feature마다 숫자 크기가 너무 달라요. 면적은 수백에서 수천인데 매끄러움은 0.0몇이거든요. 이걸 그냥 넣으면 큰 숫자가 학습을 독점해버려요. 그래서 표준화로 다 비슷한 크기로 맞췄어요."

**"표준화 공식이 어떻게 돼요?"**
💬 "값에서 평균을 빼고 표준편차로 나눠요. z-score라고 부르는 표준 방법이에요. 평균을 빼면 중심이 0이 되고, 표준편차로 나누면 퍼진 정도가 1로 맞춰져서, 모든 feature가 평균에서 표준편차 몇 칸 떨어졌나 하는 같은 단위가 돼요."

**"평균이랑 표준편차는 어느 데이터로 구했어요?"**
💬 "학습 데이터로만 구했어요. 그리고 그 값을 검증이랑 테스트에도 똑같이 적용해요. 만약 테스트 데이터로 평균을 구하면 테스트를 미리 엿본 거라 정보 누출이 되거든요. 그래서 평균이랑 표준편차를 모델이랑 같이 저장해 둬요."

**"정답은 어떻게 처리했어요?"**
💬 "one-hot으로요. 정답이 글자 M, B인데 신경망 출력은 숫자 두 개잖아요. 그래서 양성은 1과 0, 악성은 0과 1 이렇게 숫자 쌍으로 바꿔서 비교할 수 있게 했어요."

## 데이터 분리에 대해

**"데이터를 왜 셋으로 나눠요?"**
💬 "학습용은 가중치를 조정하는 데 쓰고, 검증용은 학습 중에 과적합을 보거나 early stopping에 쓰고, 테스트용은 마지막에 진짜 점수를 매기는 데 써요. 시험으로 치면 연습문제, 모의고사, 실제 시험인 거죠."

**"split이 왜 두 군데 있어요?"**
💬 "검증 데이터는 정보 누출을 막으려고 학습 데이터에서 떼어내야 해요. 제 워크플로우에선 split.py가 그 일을 하고요. 근데 평가용 evaluation.py는 검증 파일을 안 만들고 학습용이랑 테스트용만 만들어요. 그래서 train.py가 파일 하나만 받아도 내부에서 검증을 떼어낼 수 있게 했어요. 한 번 실행할 때는 둘 중 하나만 작동하고, 셔플하고 자르는 로직은 split_indices라는 함수 하나로 공유해요."

**"split.py랑 evaluation.py 차이가 뭐예요?"**
💬 "둘 다 데이터를 둘로 나누는 같은 일을 해요. split.py는 제가 만든 제출물이고, Dataset split 항목 채점용이에요. evaluation.py는 42가 제공하는 거라 제 게 아니고, 모든 학생을 똑같은 방식으로 공정하게 채점하려고 평가자가 쓰는 거예요. 그래서 git에는 안 올려요."

## 모델에 대해

**"가중치 초기값은 어떻게 정해요?"**
💬 "랜덤으로 시작해요. 0으로 하면 안 되는데, 모든 가중치가 같으면 뉴런들이 전부 똑같이 행동해서 서로 다른 걸 못 배우거든요. 이걸 대칭 문제라고 해요. 그래서 랜덤으로 대칭을 깨고요. 크기는 He 초기화로 입력 개수에 맞춰 조절해요. 루트 2 나누기 입력 개수를 곱하는데, 입력이 많으면 가중합이 커지니까 그만큼 줄여줘서 신호가 적당히 유지되게요. bias는 0으로 시작해요, 가중치가 이미 대칭을 깼으니까요."

**"bias도 학습돼요?"**
💬 "네. 0으로 시작하지만 학습 중에 가중치랑 똑같이 자기 기울기로 매 배치마다 업데이트돼요. 끝나면 0이 아닌 값이 돼요."

**"binary cross-entropy가 뭐예요?"**
💬 "둘 중 하나를 맞히는 문제에서 예측이 얼마나 틀렸는지 재는 점수예요. 정답 자리에 모델이 준 확률에 마이너스 로그를 씌우고 전체를 평균내요. 낮을수록 좋고 0이면 완벽이에요. 확신하면서 틀리면 값이 폭발하고요."

**"왜 정확도 말고 BCE를 써요?"**
💬 "정확도는 맞았냐 틀렸냐만 봐요. 근데 BCE는 얼마나 확신했는지까지 봐요. 0.51로 간신히 맞힌 거랑 0.99로 확실히 맞힌 거를, 정확도는 똑같이 보지만 BCE는 후자를 훨씬 좋게 평가해요. 그래서 더 정밀한 지표예요."

**"왜 테스트 점수가 학습 점수보다 나빠요?"**
💬 "학습 데이터는 모델이 보고 외운 거라 당연히 잘 맞혀서 낮게 나와요. 테스트는 한 번도 안 본 데이터라 진짜 실력이고 좀 더 높게 나와요. 이 차이가 사실 과적합 개념이에요. 그래서 채점은 반드시 안 본 테스트 데이터로 해야 진짜 점수예요."

**"5점이 안 나오는데요?"**
💬 "성능 점수는 evaluation.py가 만드는 split 운에 많이 좌우돼요. 쉬운 split이면 5점, 어려우면 4점이 천장일 수 있어요. 데이터가 작아서 어쩔 수 없는 부분이고, 그래서 규칙도 세 번까지 시도해서 best를 쓰게 허용해요. 제 모델은 여러 설정을 비교해봐도 이미 제일 좋은 상태예요."

---

# PART 5. 마지막 점검

- [ ] evaluation.py 인트라에서 받아뒀나
- [ ] `./venv/bin/python -c "import numpy"` 되나
- [ ] predict는 **반드시 data_test.csv** (data_training 아님!)
- [ ] PART 3 알고리즘 4개 입에 붙었나 (이게 20점)
- [ ] 학습곡선 창 닫고 다음 명령 진행하는 거 기억
